# Container registry & image tags
CONTAINER_REGISTRY=registry.gitlab.com/your-group/smilefactory
IMAGE_TAG=latest
API_GATEWAY_IMAGE=api-gateway
AUTH_SERVICE_IMAGE=auth-service
USER_SERVICE_IMAGE=user-service

# Domains
API_DOMAIN=api.example.com
TRAEFIK_DASHBOARD_DOMAIN=traefik-api.example.com

# TLS / Traefik
LETSENCRYPT_EMAIL=devops@example.com
# Generate with: htpasswd -nb <user> <password>
TRAEFIK_BASIC_AUTH=user:\$apr1\$examplehash

# Shared configuration
SUPABASE_URL=https://xyzcompany.supabase.co
SUPABASE_SERVICE_KEY=replace-me
JWT_SECRET=replace-with-strong-secret
JWT_PUBLIC_KEY=-----BEGIN PUBLIC KEY-----\n...\n-----END PUBLIC KEY-----
SENDGRID_API_KEY=SG.replace-this

# Redis
REDIS_PASSWORD=

